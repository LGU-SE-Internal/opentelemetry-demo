apiVersion: v1
kind: Service
metadata:
  name: prometheus
  labels:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/part-of: opentelemetry-demo
spec:
  type: ClusterIP
  ports:
  - port: 9090
    targetPort: 9090
    name: http-metrics
  selector:
    app.kubernetes.io/name: prometheus
