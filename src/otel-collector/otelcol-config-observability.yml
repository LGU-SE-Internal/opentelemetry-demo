# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0

# Observability stack collector additions: Jaeger, Prometheus, and OpenSearch exporters.
# Loaded when running with compose.observability.yaml.
#
# IMPORTANT: The OTel Collector merges config files but REPLACES arrays, not appends.
# The pipeline exporters below must repeat all exporters from earlier config layers
# (e.g. debug, spanmetrics) to keep them active alongside the observability exporters.
#
# Exporter name reference (these are the stable names forks should reference):
#   traces:  otlp_grpc/jaeger
#   metrics: otlp_http/prometheus
#   logs:    opensearch
#
# If you are running with compose.full.yaml, also repeat the full receivers in metrics.

exporters:
  otlp_grpc/jaeger:
    endpoint: "jaeger:4317"
    tls:
      insecure: ${env:OTELCOL_TLS_JAEGER_INSECURE:-true}
      ca_file: ${env:OTELCOL_TLS_CA_FILE:-}
      cert_file: ${env:OTELCOL_TLS_CLIENT_CERT_FILE:-}
      key_file: ${env:OTELCOL_TLS_CLIENT_KEY_FILE:-}
      insecure_skip_verify: ${env:OTELCOL_TLS_JAEGER_INSECURE_SKIP_VERIFY:-false}
    queue_settings:
      queue_size: ${env:OTEL_COLLECTOR_EXPORTER_QUEUE_SIZE:-1000}
  otlp_http/prometheus:
    endpoint: "http://prometheus:9090/api/v1/otlp"
    tls:
      insecure: ${env:OTELCOL_TLS_PROMETHEUS_INSECURE:-true}
      ca_file: ${env:OTELCOL_TLS_CA_FILE:-}
      cert_file: ${env:OTELCOL_TLS_CLIENT_CERT_FILE:-}
      key_file: ${env:OTELCOL_TLS_CLIENT_KEY_FILE:-}
      insecure_skip_verify: ${env:OTELCOL_TLS_PROMETHEUS_INSECURE_SKIP_VERIFY:-false}
    queue_settings:
      queue_size: ${env:OTEL_COLLECTOR_EXPORTER_QUEUE_SIZE:-1000}
  opensearch:
    logs_index: otel-logs
    logs_index_time_format: "yyyy-MM-dd"
    http:
      endpoint: "http://opensearch:9200"
      tls:
        insecure: ${env:OTELCOL_TLS_OPENSEARCH_INSECURE:-true}
        ca_file: ${env:OTELCOL_TLS_CA_FILE:-}
        cert_file: ${env:OTELCOL_TLS_CLIENT_CERT_FILE:-}
        key_file: ${env:OTELCOL_TLS_CLIENT_KEY_FILE:-}
        insecure_skip_verify: ${env:OTELCOL_TLS_OPENSEARCH_INSECURE_SKIP_VERIFY:-false}
    queue_settings:
      queue_size: ${env:OTEL_COLLECTOR_EXPORTER_QUEUE_SIZE:-1000}
  otlp_grpc/firepit:
    endpoint: "firepit:4317"
    tls:
      insecure: ${env:OTELCOL_TLS_FIREPIT_INSECURE:-true}
      ca_file: ${env:OTELCOL_TLS_CA_FILE:-}
      cert_file: ${env:OTELCOL_TLS_CLIENT_CERT_FILE:-}
      key_file: ${env:OTELCOL_TLS_CLIENT_KEY_FILE:-}
      insecure_skip_verify: ${env:OTELCOL_TLS_FIREPIT_INSECURE_SKIP_VERIFY:-false}
    queue_settings:
      queue_size: ${env:OTEL_COLLECTOR_EXPORTER_QUEUE_SIZE:-1000}

service:
  pipelines:
    traces:
      exporters: [debug, otlp_grpc/jaeger, span_metrics]
    metrics:
      exporters: [debug, otlp_http/prometheus]
    logs:
      exporters: [debug, opensearch]
    profiles:
      exporters: [debug, otlp_grpc/firepit]
