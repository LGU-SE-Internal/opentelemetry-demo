apiVersion: v1
kind: Service
metadata:
  name: image-provider
  labels:
    app: image-provider
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    name: http
  selector:
    app: image-provider
