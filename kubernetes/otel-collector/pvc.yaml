apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: otel-collector-buffer
  namespace: opentelemetry-demo
  labels:
    app.kubernetes.io/name: otel-collector
    app.kubernetes.io/version: "0.100.0"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
