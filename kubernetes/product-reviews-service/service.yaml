apiVersion: v1
kind: Service
metadata:
  name: opentelemetry-demo-product-reviews
  namespace: opentelemetry-demo
  labels:
    app.kubernetes.io/name: product-reviews
    app.kubernetes.io/part-of: opentelemetry-demo
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: grpc
  selector:
    app.kubernetes.io/name: product-reviews
