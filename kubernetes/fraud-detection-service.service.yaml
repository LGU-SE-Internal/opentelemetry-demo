apiVersion: v1
kind: Service
metadata:
  name: fraud-detection-service
  namespace: demo
  labels:
    app: fraud-detection-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: grpc
  selector:
    app: fraud-detection-service
