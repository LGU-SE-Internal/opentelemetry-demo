apiVersion: v1
kind: Service
metadata:
  name: accounting-service
  labels:
    app: accounting-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: health
  selector:
    app: accounting-service
