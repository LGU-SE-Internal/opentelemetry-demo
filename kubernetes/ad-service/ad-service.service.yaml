apiVersion: v1
kind: Service
metadata:
  name: ad-service
  labels:
    app.kubernetes.io/name: ad-service
    app.kubernetes.io/part-of: opentelemetry-demo
spec:
  type: ClusterIP
  ports:
    - port: 9555
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    app.kubernetes.io/name: ad-service
