apiVersion: v1
kind: Service
metadata:
  name: flagd-ui
  labels:
    app.kubernetes.io/name: flagd-ui
    app.kubernetes.io/part-of: opentelemetry-demo
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 4000
    name: http
  selector:
    app.kubernetes.io/name: flagd-ui
